'''
var=eval(input("enter data")) #int is only used to define integer values 
print(var)  #For float we simply have to change type otherwise error may occur.
print(type(var))
#If we use eval function then we don't have to change the datatype for every value.
'''